# 2026-08-17

## full-review df-binding triage; classifiedSection exemption reversed

Michael ran the first `--kind full` review (session `4c0fb58b08b3`); three findings, all accepted and applied. The durable points:

- **The 2026-06-30 `classifiedSection` exemption is reversed.** That entry recorded classifiedSection staying on `encoding/json` deliberately (display rendering, not data persistence). Michael accepted the re-raise on 2026-08-17: the boundary's original protection had dissolved — `Classified` is plain fields, dd output is materially identical, and df v1.0.2 documents the sorted-key determinism — so the exemption bought nothing and cost a blocking finding on every full review. classifiedSection now unbinds each item through `dd.Unbind` and marshals the collected maps; per-item because `dd.Unbind` takes structs, not slices, and a wrapper struct would have changed the record's top-level array shape. The classified block's keys are now sorted — same accepted format-change class as 2026-06-30.
- **`findings.Parse` needs df v1.0.3.** The findings schema makes `suggestion` *required and nullable* by design (the reviewer must explicitly say "no suggestion"), and dd before v1.0.3 had no null semantics — `dd.BindJSON` on a `"suggestion": null` errored. df v1.0.3 shipped `+nullable` (tag form `dd:",+nullable"`, name stays on snake-case fallback) and `Suggestion` now carries it. Don't remove the tag: real reviewer output contains explicit nulls routinely.
- **go.sum for df v1.0.3 was computed direct** (`GOPRIVATE` one-shot) because sum.golang.org hadn't indexed the fresh tag yet. Nothing to fix; noted in case a later `go mod` operation is surprised.
- The `dd` no-name-tag convention from 2026-06-30 still holds everywhere: `+nullable` is a flag, not a name; all keys still come from the snake-case fallback.
